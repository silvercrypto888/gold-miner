# Build artifacts
target/
node_modules/
.next/
app/.next/

# Environment
# Real env files are ignored (may hold live addresses/secrets). The *.example
# templates ARE tracked — copy them to the real file and fill in per-network.
app/.env.local
app/.env.production
app/.env.development
app/.env
.env.local
.env.production
.env.development
.env

# Keys (sensitive)
**/*-keypair.json
*.keypair.json
app/tsconfig.tsbuildinfo
.vercel
*.json
/*.json

# Allow specific JSON files
!scripts/gold-mint-v4-info.json
!scripts/gold-mint-v3-info.json
